# OpenCode Go 用量查询配置模板
# 复制本文件为 .env 并填入真实信息（.env 不会被提交到 git）

# 必需：浏览器 opencode.ai 的 auth cookie 值
# 获取：登录 https://opencode.ai → F12 → Application → Cookies → opencode.ai → 复制 "auth"
OPENCODE_AUTH=Fe26.2**your-auth-cookie-here

# 必需：workspace id（运行 --list 可查看账号下所有 workspace）
OPENCODE_WORKSPACE_ID=wrk_your-workspace-id
