# 浏览器 platform.xiaomimimo.com 的完整 cookie 字符串（必填）
# 获取：登录 https://platform.xiaomimimo.com → F12 → Network → 刷新页面 →
#       任选请求 → Request Headers → 复制整个 "cookie:" 的值
XIAOMI_MIMO_COOKIE="api-platform_serviceToken=xxx; userId=xxx; api-platform_slh=xxx; api-platform_ph=xxx"